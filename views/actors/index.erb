<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Actors</title>
  </head>
  <body>
    <h1>Actors</h1>
    <ul>
      <% @actors.each do |actor| %>
        <li>
          <a href="actors/<%= actor["id"] %>"><%= actor["name"] %></a>
          Movies: <%= actor["count"] %>
        </li>
      <% end %>
    </ul>
  </body>
</html>
