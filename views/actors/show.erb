<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= @actor[0]["name"] %></title>
  </head>
  <body>
    <h1><%= @actor[0]["name"] %> Movies:</h1>
    <ul>
      <% @movies.each do |movie| %>
        <li><a href="/movies/<%= movie["id"] %>"><%= movie["title"] %></a> <%= movie["character"] %></li>
      <% end %>
    </ul>
    <p><a href="/actors">Back to Actor list<a/></p>
  </body>
</html>
